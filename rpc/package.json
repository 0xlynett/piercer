{
  "name": "@piercer/rpc",
  "version": "0.1.0",
  "description": "A simple, modern, and easy-to-use library for bidirectional JSON-RPC over pluggable transports.",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^20.12.12",
    "typescript": "^5.9.3"
  }
}
